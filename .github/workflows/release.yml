name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgomp1

      - name: Build AMD64
        if: matrix.arch == 'amd64'
        run: |
          go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" -o chough-linux-amd64 ./cmd/chough
          tar czf chough_${GITHUB_REF_NAME}_Linux_x86_64.tar.gz chough-linux-amd64

      - name: Build ARM64
        if: matrix.arch == 'arm64'
        run: |
          sudo apt-get install -y gcc-aarch64-linux-gnu
          export CC=aarch64-linux-gnu-gcc
          export CGO_ENABLED=1
          export GOARCH=arm64
          go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" -o chough-linux-arm64 ./cmd/chough
          tar czf chough_${GITHUB_REF_NAME}_Linux_arm64.tar.gz chough-linux-arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: chough-linux-${{ matrix.arch }}
          path: chough_${GITHUB_REF_NAME}_*.tar.gz

  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build AMD64
        if: matrix.arch == 'amd64'
        run: |
          export GOARCH=amd64
          export CGO_ENABLED=1
          go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" -o chough-darwin-amd64 ./cmd/chough
          tar czf chough_${GITHUB_REF_NAME}_Darwin_x86_64.tar.gz chough-darwin-amd64

      - name: Build ARM64
        if: matrix.arch == 'arm64'
        run: |
          export GOARCH=arm64
          export CGO_ENABLED=1
          go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" -o chough-darwin-arm64 ./cmd/chough
          tar czf chough_${GITHUB_REF_NAME}_Darwin_arm64.tar.gz chough-darwin-arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: chough-darwin-${{ matrix.arch }}
          path: chough_${GITHUB_REF_NAME}_*.tar.gz

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build
        run: |
          go build -ldflags="-s -w -X main.version=${env:GITHUB_REF_NAME}" -o chough-windows-amd64.exe ./cmd/chough
          Compress-Archive -Path chough-windows-amd64.exe -DestinationPath chough_${env:GITHUB_REF_NAME}_Windows_x86_64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: chough-windows-amd64
          path: chough_${GITHUB_REF_NAME}_Windows_x86_64.zip

  release:
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          merge-multiple: true

      - name: Create checksums
        run: |
          cd ./artifacts
          sha256sum chough_${GITHUB_REF_NAME}_*.tar.gz chough_${GITHUB_REF_NAME}_*.zip > checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./artifacts/*
          generate_release_notes: true
          draft: false
          prerelease: false
